name: stayinformed_speiseplan

on:
  workflow_dispatch:
    inputs:
      publish:
        description: Image veröffentlichen
        required: true
        type: boolean
        default: false

permissions: {}

concurrency:
  group: stayinformed_speiseplan-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    uses: ./.github/workflows/reusable-addon-build.yml
    with:
      app_directory: stayinformed_speiseplan
      architectures: '["aarch64", "amd64"]'
      image_name: stayinformed_speiseplan
      publish: ${{ inputs.publish }}
    secrets: inherit
