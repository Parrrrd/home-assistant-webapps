name: kicktipp_tipbot

on:
  workflow_dispatch:
    inputs:
      publish:
        description: Image veröffentlichen
        required: true
        type: boolean
        default: false

permissions: {}

concurrency:
  group: kicktipp_tipbot-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    permissions:
      contents: read
      packages: write
      id-token: write
    uses: ./.github/workflows/reusable-addon-build.yml
    with:
      app_directory: kicktipp_tipbot
      architectures: '["amd64"]'
      image_name: kicktipp_tipbot
      publish: ${{ inputs.publish }}
    secrets: inherit
