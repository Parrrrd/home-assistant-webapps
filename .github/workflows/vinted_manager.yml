name: vinted_manager

on:
  workflow_dispatch:
    inputs:
      publish:
        description: Image veröffentlichen
        required: true
        type: boolean
        default: false

permissions: {}

concurrency:
  group: vinted_manager-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    uses: ./.github/workflows/reusable-addon-build.yml
    with:
      app_directory: vinted_manager
      architectures: '["aarch64", "amd64"]'
      image_name: vinted_manager
      publish: ${{ inputs.publish }}
    secrets: inherit
