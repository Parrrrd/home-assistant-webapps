#!/bin/sh
# Der Prozess startet im regulären WebApp-Dienst, damit Home Assistant ihn bei
# Konfigurationsänderungen zuverlässig neu erstellt. Der Dienstname bleibt für
# bestehende Add-on-Installationen erhalten.
exec sleep infinity
